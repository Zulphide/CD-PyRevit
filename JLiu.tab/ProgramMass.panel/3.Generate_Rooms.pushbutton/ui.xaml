<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generate Rooms from Mass Families"
        Height="620" Width="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="110"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Level selection -->
        <TextBlock Grid.Row="0" Text="Select level(s) to process"
                   FontWeight="SemiBold" Margin="0,0,0,6"/>

        <ListBox Grid.Row="1" x:Name="lb_levels"
                 SelectionMode="Multiple"
                 Margin="0,0,0,12"
                 BorderThickness="1"
                 BorderBrush="#CCCCCC"/>

        <!-- Parameter mapping -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Parameter mapping (mass → room)"
                       FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button x:Name="btn_add_row" Content="+ Add Row"
                    Width="80" Height="24" Margin="12,0,0,0"
                    Click="add_row_click" FontSize="11"/>
            <Button x:Name="btn_remove_row" Content="- Remove"
                    Width="80" Height="24" Margin="4,0,0,0"
                    Click="remove_row_click" FontSize="11"/>
        </StackPanel>

        <DataGrid Grid.Row="3"
                  x:Name="dg_mapping"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#CCCCCC"
                  Margin="0,0,0,12"
                  RowHeight="28"
                  ColumnHeaderHeight="30">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="Mass Parameter (source)"
                                        SelectedItemBinding="{Binding MassParam, UpdateSourceTrigger=PropertyChanged}"
                                        Width="*"/>
                <DataGridComboBoxColumn Header="Room Parameter (target)"
                                        SelectedItemBinding="{Binding RoomParam, UpdateSourceTrigger=PropertyChanged}"
                                        Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="4" x:Name="tb_mass_count"
                   Text="Masses found: 0"
                   Foreground="Gray" FontStyle="Italic"
                   Margin="0,0,0,12"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Generate Rooms" Width="120" Height="32"
                    IsDefault="True" Click="run_click"/>
            <Button Content="Cancel" Width="90" Height="32"
                    Margin="8,0,0,0" IsCancel="True" Click="cancel_click"/>
        </StackPanel>
    </Grid>
</Window>
